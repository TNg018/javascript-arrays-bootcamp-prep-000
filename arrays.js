chocolateBars = ['snickers', 'hundred grand', 'kitkat', 'skittles']

var chocolateBars = ['snickers', 'hundred grand', 'kitkat', 'skittles']

function addElementToBeginningOfArray() {
  return chocolateBars.unshift('element', 1)
}
